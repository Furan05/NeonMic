# macOS
.DS_Store

# Local agent instructions — kept out of the repo
CLAUDE.md

# Xcode
DerivedData/
xcuserdata/
*.xcuserstate

# Generated by XcodeGen (cd App && xcodegen)
App/NeonMic.xcodeproj/
App/Info.plist

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Local song library: charts embed copyrighted lyrics, audio is licensed
# content — personal, never committed. (library/* instead of library/ so
# the README below can be re-included: git cannot negate a file inside a
# fully ignored directory.)
library/*
!library/README.md
